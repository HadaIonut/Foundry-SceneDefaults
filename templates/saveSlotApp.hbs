<form>
  <div style="width: 500px; height: 100px" class="flex-column">

    <div class="form-group">
      <label>Your Saved Configs:</label>
      <div class="form-fields">
        <select name="saves" id="saves">
          <option value="default">Select a saved configs to override</option>
          {{#each saveSlots}}
            <option value="{{this}}">{{this}}</option>
          {{/each}}
        </select>
      </div>
    </div>

    <div class="form-group">
      <label>Configs Name: </label>
      <div class="form-fields">
        <input name="saveSlotName" id="saveSlotName" type="text"/>
        <button type="submit">Save</button>
      </div>
    </div>

  </div>
</form>
